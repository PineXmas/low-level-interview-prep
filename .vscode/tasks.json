{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Build all",
      "type": "shell",
      "command": "bash",
      "args": ["-lc", "for d in ${workspaceFolder}/problems/*/; do if [ -f \"$d/Makefile\" ]; then echo Building $d; (cd \"$d\" && make); fi; done"],
      "problemMatcher": ["$gcc"],
      "group": {
        "kind": "build",
        "isDefault": true
      }
    },
    {
      "label": "Clean all",
      "type": "shell",
      "command": "bash",
      "args": ["-lc", "for d in ${workspaceFolder}/problems/*/; do if [ -f \"$d/Makefile\" ]; then echo Cleaning $d; (cd \"$d\" && make clean); fi; done"],
      "problemMatcher": ["$gcc"]
    }
  ]
}
